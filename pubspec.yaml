name: a5bark
description: "A News App Not Ready yet !"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_native_splash: ^2.4.8
  provider: ^6.1.5+1
  google_fonts: ^8.1.0
  json_annotation: ^4.12.0
  get_time_ago: ^2.4.0
  cached_network_image: ^3.4.1
  easy_localization: ^3.0.8
  flutter_svg: ^2.3.0
  dropdown_button2: ^2.3.9
  dio: ^5.10.0
  pretty_dio_logger: ^1.4.0
  retrofit: ^4.9.2
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  retrofit_generator: ^10.2.7
  build_runner: ^2.15.1
  json_serializable: ^6.14.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/translations/